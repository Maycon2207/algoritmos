using System;

class DeclaracaoVariaveis
{
    static void Main()
    {
        // Declaração da variável
        int a;

        // Inicialização da variável
        a = 13;
        Console.WriteLine(a);

        // Reatribuição de valor na variável
        a = 53;
        Console.WriteLine(a);

        // Declarando e inicializando outra variável
        int b = 925;
        Console.WriteLine(b);
		
		int y = 10, x = 20;
		int z = y + x;
		Console.WriteLine(z);
    }
}
